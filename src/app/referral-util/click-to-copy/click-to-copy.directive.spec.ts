import { ClickToCopyDirective } from './click-to-copy.directive';

describe('ClickToCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickToCopyDirective();
    expect(directive).toBeTruthy();
  });
});
